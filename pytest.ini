[pytest]
pythonpath = .
testpaths = tests
markers =
    e2e: browser end-to-end tests requiring Playwright + local app startup
addopts =
    --cov=backend
    --cov-branch
    --cov-report=term-missing
    --cov-fail-under=69

[coverage:run]
branch = True
source =
    backend
